/**
 * Formatting utilities for Pathfinder 2e item display fields.
 */

/** Format a usage string for display (identity function since AoN provides readable strings). */
export function formatUsage(usage: string): string {
  return usage;
}
